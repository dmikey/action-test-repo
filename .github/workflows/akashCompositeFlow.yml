name: Build and Publish Image
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Akash On Github Actions
        uses: dmikey/akash-on-gh-actions@v1.0.87
